<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose 全权接管界面，XML 主题只需无 ActionBar -->
    <style name="Theme.Assistant" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
