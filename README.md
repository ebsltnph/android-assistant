# 随身助手（Android AI 助手）

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

一个跑在你手机上的 AI 助手 App：聊天（含联网搜索）、记日记、定时提醒、关注新闻、清晨简报、长期记忆，全部数据保存在手机本地。

## 已实现功能

- 🌌 **深墨夜景主题**：全 App 统一 glassmorphism 风格（深墨底 + 白色玻璃卡片 + 香槟金强调色，与浮动界面一致）
- 🏠 **首页仪表盘**：悬浮球开关 + **长期记忆入口** + 清晨简报 / 昨日小结气泡（点击查看）+ 最近提醒（直接显示 4 条）+ 事件监控列表，全部玻璃气泡卡片
- 💬 **AI 聊天**：流式回复（逐字显示）、多轮上下文（长度可在设置里调）、推理模型思考过程分开展示且**可点击展开/收起**、**复制 / 重做**（图标按钮，聊天页与浮动界面均有）
- 🔧 **主模型统一工具调度**：提醒、记录、长期记忆、事件监控、联网搜索、**网页全文阅读**全部由主聊天模型在对话中自主决定调用（可连环调用、失败自纠重试），不再各自独立判断——一次回答内最多 4 轮工具调用
- 📔 **日记**：单一「日记」本；聊天里说"记录一下…"**自动整理后**入日记（LLM 结合对话与回复总结，能看到主聊天模型的回复）；**支持图片**——聊天发图+「记录」或悬浮球识屏+「记录」自动带图，日记页可补图/看大图、**单条编辑**；写完自动抽取长期记忆
- 🏷️ **日记标签**：一条日记可打多个标签；标签词汇表由用户自定义（默认：工作 / 生活 / 待办 / 经验）；聊天记录时由 AI 在词汇表内选 0-3 个标签，日记页手动记录可自己选；支持「未分类」只看无标签日记、多标签“且”筛选
- 🧠 **长期记忆**：LLM 判断重要性（评分 ≥7 才存），记住重要事实（如"以后叫我龙"），每次对话自动注入；**首页独立入口**，支持手动添加、单条编辑、删除、清空（已从日记页独立出来）
- 📅 **每日小结**：每天定时自动汇总最近 24h 日记生成小结推送通知；时间可精确到分钟；历史落库可回看；同步到系统日历；通知点击看完整内容
- 📆 **期间总结**：日记页一键生成**指定时间段**的日记总结（自选起止日期，Material3 日历）；生成后自动保留**最近 5 条**历史可随时回看；总结可一键**复制**到剪贴板或**导出**（系统分享，可存文件/发到其他应用）
- ⏰ **定时提醒**：聊天说"提醒我明天下午3点开会"自动创建；支持每天/每周重复、开机恢复（**错过触发后自动补排下一次，不会失效**）；**触发后自动记入日记**；**通知需点击确认（弹窗手动确认），未确认每 5 分钟重复提醒**；过期提醒自动清理
- 📰 **新闻事件监控**：说"关注 XX 新闻"→ 定期搜索，有重要动态时通知（24h 去重）；**可自定义判断规则和限定来源域名**、调整检查周期（1-168 小时）
- 🌅 **清晨简报**：每天定时推送（默认 7:30 可精确到分钟）：今日提醒 + 昨日小结；**首页随时可看**、通知点击弹窗
- 🌙 **免打扰**：默认 23:00-07:00（可精确到分钟），期间提醒静默、事件监控不打扰
- 📖 **使用说明**：设置页顶部内置完整功能说明，新用户可快速了解聊天、记录、提醒、识屏、悬浮球、备份等能力
- ✍️ **文本划词**：App 与浮动界面中的聊天、日记、记忆、提醒、事件、小结等文字支持长按选择/复制
- ⏰ **提醒编辑**：提醒页可对单条提醒编辑标题/时间/重复，保存后自动重排闹钟
- 📅 **每日小结 / 清晨简报可开关**：设置页可独立关闭或开启自动任务，关闭后仍可手动生成/查看历史
- 👁️ **智能识屏**：聊天说「识屏」/ 点「识屏」磁贴 / 悬浮球选「识屏」→ 截屏 → **浮动界面**弹出（提取文字/翻译/总结内容 + 带图对话），结果自动留存到聊天记录
- 🖱️ **识屏框选**：截屏后先弹出框选层（背景与浮动界面同款），拖动选框只识别框内区域，可拖四角微调、选框不会超出截图；不框选直接确定 = 识别全屏；设置页可一键关闭回到整屏识别
- 🫧 **悬浮球 + 浮动界面**：设置页开启后，一颗玻璃拟态小球常驻任意应用上层（可拖拽贴边）；点击展开**浮动界面**——深墨玻璃拟态 + 动态多色光晕，四个功能气泡（**识屏 / 提醒 / 记录 / 对话**）+ 玻璃输入框；与 App 聊天**同一会话**，对话、识屏结果自动留存一份到聊天记录；系统返回手势直接退出回原应用
- 🖼️ **图片分析**：分享图片到助手 / 聊天点 + 上传图片 → 附件栏等待，输入要求后**文字+图片一起发给视觉模型**（如"翻译这张图"）
- ⚙️ **模型配置**：每个提供商可单独**测试连接**、单独设置**思考深度**（OpenAI 通用参数 reasoning_effort，省 token 加速）；对话/识屏/分类能力指派；**聊天上下文长度可调**（5-50 轮）
- 📝 **提示词可编辑**：12 组提示词；常用两组（助手系统提示词 / 识屏提示词）直接编辑，其余 10 组在「高级设置」里编辑或恢复默认
- 🔌 **自带模型配置**：应用内填 base URL + API Key 即可接入 DeepSeek、通义、Kimi、OpenAI 等 OpenAI 兼容接口；对话与识屏可配置不同提供商（识屏需支持图片输入的模型）
- 🖼️ **日记多图**：一条日记可添加多张图片（相册多选、一次最多 9 张）；缩略图点 ✕ 删单张、点图看大图、**一键下载到系统相册**
- 📰 **事件监控详情 + 触发历史**：点事件卡片打开详情窗口（完整配置 + 每次命中记录的时间/标题/内容/原文链接）；命中通知点击直接跳到详情
- 🫙 **秘密功能**（设置页最底部）：自动记录**所有你发出的对话内容**（不含模型回复，只存本机），可导出/清空——为后续制作你的数字分身准备素材
- ➗ **数学公式渲染**：聊天/识屏回复里的 LaTeX 公式（`$…$`、`$$…$$`、`\(…\)`、`\[…\]`，含矩阵/分式/求和）渲染成清晰公式显示；**基础 Markdown** 也支持（加粗/斜体/行内代码/标题/列表）
- 💾 **数据备份与导入**：一键导出全部数据（设置/日记+图片/记忆/提醒/事件/小结/对话历史，**不含 API Key**）到备份文件，可随时恢复（覆盖式 + 自动重启）；**定期自动备份**可选每天/3 天/周，自动存到手机「下载」文件夹（卸载不丢，保留最近 3 份）

## 开发状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 | 开发环境搭建 | ✅ 完成 |
| P1 | 项目骨架 + 底部导航 5 页 | ✅ 完成 |
| P2 | 设置 + 文字聊天（流式回复、多轮上下文） | ✅ 完成 |
| P3 | 日记 + 长期记忆 + 每日小结（可配置时间/历史落库/系统日历） | ✅ 完成 |
| P4 | **对话搜索 + 定时提醒 + 事件监控 + 清晨简报 + 免打扰** | ✅ 完成 |
| P5 | **智能识屏 + 分享到助手 + 识屏悬浮小窗** | ✅ 完成 |
| P6 | **悬浮球 + 浮动界面**（识屏/提醒/记录/对话全功能悬浮调用，玻璃拟态 UI） | ✅ 完成 |
| UI | **主界面改版**（深墨夜景主题统一、首页仪表盘、设置页重构、思考强度 per-provider） | ✅ 完成 |
| v1.1 | **记录/日记/提醒/设置优化**（记录走 LLM 总结、日记图片、合并单本、提醒重复修复、上下文长度设置、思考展开收起、提示词对齐） | ✅ 完成 |
| v1.2 | **日记多图 + 事件详情/触发历史 + 秘密功能 + 启动闪退/旋转/提醒确认修复**（见下方 v1.2.0 说明） | ✅ 完成 |
| v1.2.3 | **数学公式 + 基础 Markdown 渲染**（jlatexmath 本地渲染，聊天/识屏回复里的 LaTeX 公式、矩阵、分式） | ✅ 完成 |
| v1.3 | **数据备份与导入 + 定期自动备份**（手动导出/恢复、自动备份到下载目录） | ✅ 完成 |
| v1.3.1 | **指定期间日记总结**（自选起止日期汇总、历史保留最近 5 条、复制/导出） | ✅ 完成 |
| v1.4.0 | **日记标签 + 长期记忆独立入口 + 编辑能力 + 划词 + 开关 + 使用说明 + 提示词保存修复**（见下方说明） | ✅ 完成 |
| v1.5.0 | **主模型统一工具调度 + 网页阅读 + 五档思考深度 + 界面焕新**（见下方说明） | ✅ 完成 |
| v1.6.0 | **语音输入三模式（键盘/系统听写/远程识别 API）+ TTS 朗读 + 读日记工具 + 编辑重发 + 悬浮球图标自定义 + 朝向修复**（见下方说明） | ✅ 完成 |
| P7 | 真·语音唤醒词（可选） | 待开发 |

**v1.6.0**（2026-08-25）：**语音输入全面升级 + 语音朗读 + 日记检索 + 编辑重发**——
**🎙️ 悬浮球语音输入三模式**（设置 → 悬浮球自由切换）：键盘语音（默认，弹键盘后点键盘麦克风）/ 系统听写（部分机型支持）/ **远程识别**（录音上传到你指派的语音识别模型，说完停顿自动识别并发送；兼容 OpenAI 语音转文字接口，支持硅基流动 Qwen3-ASR、SenseVoice 等）；浮动面板输入框旁新增金色 🎙️ 按钮随时语音；
**🔊 语音朗读回复**：每条回复旁有喇叭按钮点击朗读（再点停止、点别的切换）；对助手说「读出来」它也会自主提炼要点朗读；
**📔 读日记工具**：对助手说「帮我找找上周记的 XX」「看看标记为工作的日记」——按关键词/标签/天数检索历史日记；
**✏️ 编辑重发**：最后一条消息（含带图）旁有编辑图标，点击撤回该轮、文字回到输入框，改完重发；
**🎨 悬浮球图标自定义**：emoji 或相册图片（自动裁圆铺满球体），实时生效；
**🔄 浮动界面朝向修复**：打开时与前台应用朝向一致，且实时跟随系统自动旋转开关；
⚠️ 升级注意：语音识别的远程模式需在 设置 → 模型配置 → 能力指派 中把「语音识别」指派给支持音频转文字的模型（编辑提供商时打开「支持语音识别」开关）；数据与备份完全兼容，直接覆盖安装即可。
**v1.5.0**（2026-08-24）：**主模型统一工具调度架构 + 网页全文阅读 + 界面视觉焕新**——
**🔧 主模型统一工具调度**：提醒、记录、长期记忆、事件监控、联网搜索、网页阅读、识屏全部由主聊天模型在对话中自主决定调用（`[调用]` 协议，单次回复最多 4 轮、失败自动重试），删除了意图分类/记忆抽取/日记整理/监控抽取/时间解析五处独立 LLM 判断调用，每条消息更快更省；
**🌐 网页全文阅读**：搜索摘要不够时模型会自己打开来源网页读全文再回答（Tavily Extract）；
**🖼️ 识屏框选**：截屏后先框选、只识别框内区域（四角微调、不框选=全屏，默认开启）；
**🎚️ 思考深度五档**：极简/低/中/高/极高（xhigh 超范围自动降级），设置页显示参数实际生效状态；
**💬 分段时间线气泡**：多轮工具过程中思考块/正文/工具执行行按真实顺序全程可见；带图记录改走静默整理回路（自动总结+选标签+附图）；
**🎨 界面视觉焕新**：全 App 环境光背景（深墨渐变+氛围光晕）、半透明玻璃导航栏（选中态香槟金点亮）、首页时段问候语+日期、聊天气泡不对称圆角玻璃质感、圆角输入栏与金色发送键、空状态英雄区；全主题文字对比度提亮。
⚠️ 升级注意：助手系统提示词默认值已更新对齐新架构——此前自定义过该提示词的用户请在 设置 → 提示词 → 助手系统提示词 恢复默认以获得最佳效果；数据与备份完全兼容，直接覆盖安装即可。
**v1.4.0**：**日记标签 + 长期记忆独立入口 + 编辑能力 + 划词 + 开关 + 使用说明 + 提示词保存修复**——修复提示词编辑框“保存有时不成功”的协程取消问题；App 与浮动界面主要文字支持长按划词/复制；日记新增**用户自定义标签系统**（AI 从词汇表选 0-3 个标签、多标签“且”筛选、未分类、标签横向滑动展示），日记页改为紧凑布局（右上角“+写日记”、放大镜展开搜索）；长期记忆从日记页移到**首页独立入口**；日记条目可单条编辑，提醒支持编辑标题/时间/重复并自动重排；每日小结与清晨简报可在设置中独立开关；设置页新增“使用说明”入口；悬浮球“识图”按钮统一改为“识屏”；默认提示词小幅优化。
**v1.3.1**（2026-08-11）：**指定期间日记总结**——日记页「期间总结」自选起止日期（Material3 日历，起止颠倒自动互换），把该时间段的日记汇总成一份总结；生成后自动保留**最近 5 条**历史可随时回看；总结支持一键**复制**到剪贴板、一键**导出**（系统分享，可存文件或发到其他应用）；生成过程中持续显示进度提示（点号动态变化，不依赖系统动画）。
**v1.3.0**（2026-08-10）：**数据备份与导入**——一键导出全部数据（设置/日记+图片/记忆/提醒/事件/小结/对话历史，**不含 API Key**）到 ZIP 备份文件，可预览确认后覆盖恢复并自动重启；**定期自动备份**（设置页开关，可选每天/每 3 天/每周，凌晨 2 点执行，自动存到手机「下载」文件夹——卸载不丢，保留最近 3 份）；**数学公式渲染**：聊天/识屏回复里的 LaTeX 公式（行内 `$…$` / 块级 `$$…$$`、`\(…\)`、`\[…\]`，含矩阵、分式、求和等）渲染成清晰公式，基础 Markdown（加粗/斜体/行内代码/标题/列表）同步支持。
**v1.2.1**（2026-08-07）：思考参数改 **OpenAI 通用格式**——只发 `reasoning_effort`（深度），不再发送 DeepSeek 专属的 `thinking` 开关，兼容中转站/OpenAI 系模型（识屏报 `Unknown parameter: 'thinking'` 修复），设置界面合并为「思考深度」一个下拉；**主界面固定竖屏**（不再随手机旋转，浮动界面旋转行为不变）；秘密功能统计修复、荣耀旋转锁兼容、日记缩略图删除按钮优化。
**v1.2.0**（2026-08-07）：日记一条目多张图片（多选添加/单张删除/下载到相册）；事件监控**详情窗口 + 触发历史**（命中自动记录，通知点击直达）；**秘密功能**（对话历史记录与导出，数字分身素材）；修复**重启后快速打开 App 闪退**（启动期不再阻塞主线程 + 前台服务安全启动 + 数据损坏自动恢复）、**系统关闭自动旋转后界面仍旋转**（荣耀 ROM 兼容）、**每日提醒确认后仍重复提醒**（确认竞态修复）；启动时自动清理缓存（孤儿日记图片 + 过期识屏截图）。
**v1.1.0**（2026-08-02）：记录与日记改造 + 提醒修复 + 设置增强，已推送 GitHub。
**v1.0.0 正式版**（2026-08-02）：第一阶段功能全部完成，发布安装包（GitHub Releases）。

## 安装指南（v1.6.0）

### 方式一：直接安装 APK（推荐）

1. 在 GitHub [Releases 页面](https://github.com/ebsltnph/android-assistant/releases) 下载最新版 `app-release.apk`
2. 把 APK 传到手机（微信/QQ 传文件、网盘、数据线复制均可），点击安装
3. 如果提示"未知来源"，按提示允许"安装未知应用"即可
4. 首次使用：打开 App → 设置 → 模型配置 → 添加提供商（填 Base URL + API Key + 模型名，如 DeepSeek `https://api.deepseek.com`）→ 测试连接 → 设为默认

> 荣耀手机提示无法安装时：设置 → 安全 → 更多安全设置 → 开启「外部来源应用」；或用开发者选项 + 数据线 `adb install`（见下方）。

### 方式二：开发者模式安装（adb）

```bash
# 手机开启开发者选项（设置 → 关于手机 → 连点"版本号"7 次）→ USB 调试
adb install -r app-release.apk
```

### 方式三：源码构建

1. 安装 [Android Studio](https://developer.android.com/studio)（官方免费，自带 JDK 和 Android SDK）
2. 用 Android Studio 打开本项目文件夹（`File → Open`），等待 Gradle 同步完成
3. 手机开启开发者选项 + USB 调试，连接手机
4. 点 Android Studio 的 ▶ Run 按钮（调试版）；或终端 `./gradlew assembleRelease` 生成正式版 APK

## 使用说明

1. **设置 → 模型配置**：添加提供商（填名称、Base URL、API Key、模型名）；每个提供商可单独「测试连接」、单独设置思考深度；下方「能力指派」可分别指定对话/识屏/分类模型
2. **设置 → 搜索（联网）**：默认 keyless 免费模式，点「填入 API Key」可填 Tavily Key（每月 1000 次免费，tavily.com 注册）
3. **设置 → 每日小结 / 清晨简报 / 免打扰**：列表直接显示设定时间，点进去改（时间均可精确到分钟）
4. **聊天**：直接打字问问题；涉及实时信息（新闻/价格/天气等）时助手会自行联网搜索、必要时还能打开搜索结果里的网页读全文；说"记录一下…"自动整理后写入日记（词汇表内选标签）；说"提醒我…"创建提醒（时间算错会自我纠正）；说"关注…"创建事件监控；说"识屏"截屏分析
5. **识屏**：① 聊天说「识屏」→ 授权 → 截屏后弹出**框选层**（拖动选框只识别框内，可拖四角微调；不框选直接确定 = 全屏；设置页可关）→ 确认后**浮动界面**弹出（提取文字/翻译/总结内容 + 带图对话，结果自动留存聊天记录）；② 下拉通知栏 → 编辑磁贴 → 添加「识屏」→ 在任意 App 里下拉一键识屏；③ 分享图片到助手或聊天点 + 上传 → 附件栏等命令，输入要求后与图片一起发送给视觉模型
6. **悬浮球**：设置 → 悬浮球 → 开启（需悬浮窗权限）或首页顶部开关；在任意应用点小球 → 浮动界面：选「识屏」截屏识别，选「提醒」「记录」直接创建，选「对话」或直接输入聊天；系统返回手势退出
7. **日记**：写日记、点「+」给条目补图、点缩略图看大图；点「今日小结」立即生成当天总结（只总结文字内容）；点「期间总结」自选起止日期生成某段时间的总结（可回看最近 5 条、复制、导出）
8. **提醒页**：提醒列表 + 手动添加（重复可选每天/每周，选过去时间自动推到下一次）；事件监控 tab 管理关注事件（周期/规则/域名）
9. **设置 → 识屏模型**：模型配置页底部显示视觉模型状态（DeepSeek 官方 API 不支持图片，需指派通义 qwen-vl / 智谱 GLM-4V / Kimi vision 等）
10. **设置 → 提示词**：常用两组直接编辑；「高级设置」里可编辑其余 10 组或恢复默认

## 技术说明

- Kotlin + Jetpack Compose（Material 3），单模块工程
- 模型接入：统一 OpenAI 兼容 API 契约（Retrofit + kotlinx-serialization），`ProviderRegistry` 按能力解析提供商
- 工具调度：`AssistantTool` 接口 + `ToolRegistry` 注册表，主模型经文本协议 `[调用] {json}` 在回复中发起，Agent.chatReplyFlow 统一回路执行与结果回传；搜索/网页阅读走 `SearchClient` / `PageReader`（Tavily 实现，可替换），事件监控独立轮询
- 持久化：Room（日记/记忆/提醒/事件/小结）+ DataStore（设置/提示词）+ 加密存储（API Key）
- 后台任务：WorkManager 周期任务（每日小结/事件轮询/清晨简报/自动备份）+ AlarmManager 精确闹钟（提醒）
- 数学公式渲染：jlatexmath（JitPack `com.github.rikkahub:jlatexmath-android`）本地渲染 LaTeX → 位图，Compose `inlineContent` 内嵌行内公式、块级公式独立一行居中显示
- 数据备份：ZIP（`backup.json` + `images/` + `secret_log/`），SAF 免权限导出/恢复；自动备份走 MediaStore 写公共「下载」目录（API 29+ 免权限）
- 数据全部保存在手机本地（不联网上传你的日记和记忆，只调用你自己配置的模型 API；备份文件由你自己保管）

## 许可证

[MIT License](LICENSE) © 2026 ebsltnph
