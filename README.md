# 随身助手（Android AI 助手）

一个跑在你手机上的 AI 助手 App：聊天（含联网搜索）、记日记、定时提醒、关注新闻、清晨简报、长期记忆，全部数据保存在手机本地。

## 已实现功能

- 💬 **AI 聊天**：流式回复（逐字显示）、多轮上下文、推理模型思考过程分开展示、**复制回复 / 重做回复**
- 🔍 **联网搜索**：问"查一下…""最新消息"时自动联网搜索（Tavily，免注册可用），基于搜索结果回答并注明来源
- 📔 **日记**：生活/工作多日记本，聊天里说"记录一下…"或直接在日记页写；写完自动抽取长期记忆
- 🧠 **长期记忆**：LLM 判断重要性（评分 ≥7 才存），记住重要事实（如"以后叫我龙"），每次对话自动注入
- 📅 **每日小结**：每天定时自动汇总最近 24h 日记生成小结推送通知；时间可精确到分钟；历史落库可回看；同步到系统日历；通知点击看完整内容
- ⏰ **定时提醒**：聊天说"提醒我明天下午3点开会"自动创建；支持每天/每周重复、开机恢复；**触发后自动记入日记**；过期提醒自动清理
- 📰 **新闻事件监控**：说"关注 XX 新闻"→ 定期搜索，有重要动态时通知（24h 去重）；**可自定义判断规则和限定来源域名**、调整检查周期（1-168 小时）
- 🌅 **清晨简报**：每天定时推送（默认 7:30 可精确到分钟）：今日提醒 + 昨日小结；**首页随时可看**、通知点击弹窗
- 🌙 **免打扰**：默认 23:00-07:00（可精确到分钟），期间提醒静默、事件监控不打扰
- ⚙️ **高级设置**：推理模型思考开关 + 思考深度调节（省 token 加速）
- 📝 **提示词可编辑**：9 组提示词（助手/记忆/小结/分类/搜索判断/时间解析/事件等）全部可在设置页编辑或恢复默认
- 🔌 **自带模型配置**：应用内填 base URL + API Key 即可接入 DeepSeek、通义、Kimi、OpenAI 等 OpenAI 兼容接口；对话与识屏可配置不同提供商

## 开发状态

| 阶段 | 内容 | 状态 |
|---|---|---|
| P0 | 开发环境搭建 | ✅ 完成 |
| P1 | 项目骨架 + 底部导航 5 页 | ✅ 完成 |
| P2 | 设置 + 文字聊天（流式回复、多轮上下文） | ✅ 完成 |
| P3 | 日记 + 长期记忆 + 每日小结（可配置时间/历史落库/系统日历） | ✅ 完成 |
| P4 | **对话搜索 + 定时提醒 + 事件监控 + 清晨简报 + 免打扰** | ✅ 完成 |
| P5 | 智能识屏 + 分享到助手 | 待开发 |
| P6 | 悬浮球 + 快捷磁贴 + 桌面小部件 | 待开发 |
| P7 | 真·语音唤醒词（可选） | 待开发 |

## 如何构建运行（新手向）

1. 安装 [Android Studio](https://developer.android.com/studio)（官方免费，自带 JDK 和 Android SDK）
2. 用 Android Studio 打开本项目文件夹（`File → Open`）
3. 等待首次 Gradle 同步完成（会自动下载依赖）
4. 手机开启开发者选项 + USB 调试（设置 → 关于手机 → 连点"版本号"7 次 → 开发者选项 → USB 调试）
5. 连接手机，点 Android Studio 的 ▶ Run 按钮

## 使用说明

1. **设置 → 添加提供商**：填名称、Base URL（如 `https://api.deepseek.com`）、API Key、模型名，保存并设为默认
2. **设置 → 能力指派**：对话、识屏（未来）、意图分类可分别指定模型；**高级设置**可调思考开关/深度
3. **设置 → 搜索（联网）**：可留空（免费模式）或填 Tavily API Key（每月 1000 次免费，tavily.com 注册）
4. **聊天**：直接打字问问题；说"查一下…"会联网搜索；说"记录一下…"写入日记；说"提醒我…"创建提醒；说"关注…"创建事件监控
5. **日记**：写日记、建日记本；点「今日小结」立即生成当天总结
6. **提醒页**：提醒列表 + 手动添加；事件监控 tab 管理关注事件（周期/规则/域名）
7. **设置 → 每日小结/清晨简报/免打扰**：时间均可精确到分钟
8. **设置 → 提示词**：9 组提示词可编辑或恢复默认

## 技术说明

- Kotlin + Jetpack Compose（Material 3），单模块工程
- 模型接入：统一 OpenAI 兼容 API 契约（Retrofit + kotlinx-serialization），`ProviderRegistry` 按能力解析提供商
- 联网搜索：`SearchClient` 接口 + Tavily 实现（可替换），对话搜索与事件监控共用
- 持久化：Room（日记/记忆/提醒/事件/小结）+ DataStore（设置/提示词）+ 加密存储（API Key）
- 后台任务：WorkManager 周期任务（每日小结/事件轮询/清晨简报）+ AlarmManager 精确闹钟（提醒）
- 数据全部保存在手机本地（不联网上传你的日记和记忆，只调用你自己配置的模型 API）
